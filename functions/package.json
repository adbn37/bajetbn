{
  "name": "bajetbn-functions",
  "private": true,
  "version": "0.1.0",
  "main": "lib/index.js",
  "engines": { "node": "22" },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy:staging": "firebase use staging && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "14.2.0",
    "firebase-functions": "^7.2.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
