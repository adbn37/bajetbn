/*
 * Development placeholder.
 * The production service worker is generated after every Vite build by
 * scripts/generate-service-worker.mjs so the exact hashed JavaScript and CSS
 * files referenced by dist/index.html are cached as required shell assets.
 */
